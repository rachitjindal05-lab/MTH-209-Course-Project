# 1. Install the package directly from bundled file
install.packages("gradR_1.0.0.tar.gz", repos = NULL, type = "source")

# 2. Load the library officially
library(gradR)

# 3. Launch the app
visualize_gradientdescent()
